// Populated at container startup by nginx entrypoint via envsubst.
// On GitHub Pages this file is empty — key is baked in at build time via process.env.
window.GOOGLE_MAPS_API_KEY = window.GOOGLE_MAPS_API_KEY || '';
